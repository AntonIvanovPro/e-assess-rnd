# Releases

Each release document defines the outcome, included use cases, explicit exclusions, and completion criteria for one version. Create a new release document only when its scope is ready to be decided.
